<li id="<%= feed3_item.id %>">
  
  <span class="timestamp">
    Posté le <%= feed3_item.created_at.strftime("%d, %b, %Y") %>.
  </span></br>
  <div class= "tings">
  <span class="squat">Squat:   <%= feed3_item.squat %>kgs</span></br>
  <span class="developpe_barre">Developpe barre:   <%= feed3_item.developpe_barre %>kgs</span></br>
  <span class="curl_barre">Curl barre:   <%= feed3_item.curl_barre %>kgs</span></br>
 
 </div>
 <% if current_user?(feed3_item.user) %>
    <%= link_to "annuler", feed3_item, method: :delete,
                                     data: { confirm: "vous etes sure?" },
                                     title: feed3_item %>
  <% end %>
</li>