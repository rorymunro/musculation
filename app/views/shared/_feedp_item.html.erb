<li id="<%= feedp_item.id %>">
  
  <span class="timestamp">
    Posté le <%= feedp_item.created_at.strftime("%d, %b, %Y") %>.
  </span></br>
  <div class= "tings">
  <span class="developpe_couche">Developpe couche:   <%= feedp_item.developpe_couche %>kgs</span></br>
  <span class="developpe_incline">Developpe incline:   <%= feedp_item.developpe_incline %>kgs</span></br>
  <span class="ecarte_couche">Ecarte couche:   <%= feedp_item.ecarte_couche %>kgs</span></br>
  <span class="ecarte_incline">Ecarte incline:   <%= feedp_item.ecarte_incline %>kgs</span></br>
  <span class="haltere_au_front">Haltere au front:   <%= feedp_item.haltere_au_front %>kgs</span>
 </div>
 <% if current_user?(feedp_item.user) %>
    <%= link_to "delete", feedp_item, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: feedp_item %>
  <% end %>
</li>