<li id="<%= feeds_item.id %>">
  
  <span class="timestamp">
    Posté le <%= feeds_item.created_at.strftime("%d, %b, %Y") %>.
  </span></br>
  <div class= "tings">
  <span class="squat">Squat:   <%= feeds_item.squat %>kgs</span></br>
  <span class="rowing_menton">Rowing menton:   <%= feeds_item.rowing_menton %>kgs</span></br>
  <span class="elevations_devant">Elevations devant:   <%= feeds_item.elevations_devant %>kgs</span></br>
  <span class="rowing_courbe">Rowing courbe:   <%= feeds_item.rowing_courbe %>kgs</span></br>
  
 </div>
 <% if current_user?(feeds_item.user) %>
    <%= link_to "delete", feeds_item, method: :delete,
                                     data: { confirm: "vous etes sure?" },
                                     title: feeds_item %>
  <% end %>
</li>