<li id="<%= feedpro_item.id %>">
  
  <span class="timestamp">
    Posté le <%= feedpro_item.created_at.strftime("%d, %b, %Y") %>.
  </span></br>
  <div class= "tings">
  <span class="location">Location:   <%= feedpro_item.location %></span></br>
  <span class="taille">Taille:   <%= feedpro_item.taille %>metres</span></br>
  <span class="poids">Poids:   <%= feedpro_item.poids %>kgs</span></br>
  <span class="age">Age:   <%= feedpro_item.age %>ans</span></br>
 
 </div>
 <% if current_user?(feedpro_item.user) %>
    <%= link_to "annuler", feedpro_item, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: feedpro_item %>
  <% end %>
</li>