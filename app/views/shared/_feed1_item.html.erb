<li id="<%= feed1_item.id %>">
  
  <span class="timestamp">
    Posté le <%= feed1_item.created_at.strftime("%d, %b, %Y") %>.
  </span></br>
  <div class= "tings">
  <span class="squate">Squat:   <%= feed1_item.squat %>kgs</span></br>
  <span class="souleve de terre">Souleve de terre:   <%= feed1_item.souleve_de_terre %>kgs</span></br>
  <span class="developpe_c">Developpe couche:   <%= feed1_item.developpe_couche %>kgs</span></br>
 
 </div>
 <% if current_user?(feed1_item.user) %>
    <%= link_to "annuler", feed1_item, method: :delete,
                                     data: { confirm: "vous etes sure?" },
                                     title: feed1_item %>
  <% end %>
</li>