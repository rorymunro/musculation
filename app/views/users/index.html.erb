<% provide(:title, 'All users') %>
<div class= "all"><h1>Tous les utilisateurs</h1></div>

<%= will_paginate %>

<ul class="users">

  
  
  <%= render @users.order("squatu DESC").all %>
  




</ul>

<%= will_paginate %>
